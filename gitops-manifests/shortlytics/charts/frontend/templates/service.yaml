apiVersion: v1
kind: Service
metadata:
  name: {{ include "shortlytics-chart.fullname" . }}-frontend
  labels:
    app.kubernetes.io/name: frontend
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/instance: {{ .Release.Name }}