apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "shortlytics-chart.fullname" . }}-frontend-sa
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/instance: {{ .Release.Name }}